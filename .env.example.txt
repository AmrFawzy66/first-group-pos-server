# Copy this file to ".env" for local testing (Render/Railway: set these as
# real environment variables in their dashboard instead — do NOT upload .env).

# Your MongoDB Atlas connection string. Get it from Atlas -> Connect ->
# "Drivers", then paste it here and replace <db_password> with the real
# database user password (NOT your Atlas login password).
MONGODB_URI=mongodb+srv://mmha43889_db_user:REPLACE_WITH_REAL_PASSWORD@amr.6qdxmlu.mongodb.net/?appName=amr

# Any name you like — this is where all the shop's data will live inside
# your MongoDB Atlas cluster.
DB_NAME=first_group_pos

# A long random secret ONLY you know. Generate one at
# https://www.uuidgenerator.net/ or just mash the keyboard for 30+ characters.
# This exact same value must be pasted into the app's Settings -> "مفتاح الوصول".
API_KEY=REPLACE_WITH_A_LONG_RANDOM_SECRET

# Render/Railway set this automatically — only needed for local testing.
PORT=3000
